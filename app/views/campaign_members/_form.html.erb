<%#= form_with(model: campaign_member) do |form| %>
<%= form_with(model: [@campaign, campaign_member]) do |form| %>
  <% if campaign_member.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(campaign_member.errors.count, "error") %> prohibited this campaign_member from being saved:</h2>

      <ul>
        <% campaign_member.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%#= form.text_field :user_id %>
    <%= form.select :user_id, @userList.map { |u| [u.email, u.id]} %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
